<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="namlit.slackforce"
    android:versionCode="1"
    android:versionName="v0.1">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SelectWebbingManufacturerActivity"
            android:label="@string/title_activity_select_webbing_manufacturer_action" >
        </activity>
        <activity
            android:name=".SelectWebbingActivity"
            android:label="@string/title_activity_select_webbing" >
        </activity>
    </application>

</manifest>
